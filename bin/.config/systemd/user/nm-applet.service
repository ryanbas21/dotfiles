[Unit]
Description=NetworkManager Applet (tray)
After=graphical-session.target polybar.service
Wants=graphical-session.target polybar.service

[Service]
ExecStart=/usr/bin/nm-applet --indicator
Restart=on-failure

[Install]
WantedBy=xsession.target
