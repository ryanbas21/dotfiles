#!/bin/sh

# Start dbus if not already
if [ -z "$DBUS_SESSION_BUS_ADDRESS" ]; then
  eval "$(dbus-launch --sh-syntax --exit-with-session)"
fi

# All startup apps (picom, polybar, dunst, etc.) are handled by
# xmonad's myStartupHook in lib/Config.hs via spawnOnce

exec xmonad
